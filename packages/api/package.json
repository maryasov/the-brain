{
  "name": "@the-brain/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local HTTP JSON API over the brain graph for scripts and agents (no framework).",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "@the-brain/core": "workspace:*",
    "@the-brain/db": "workspace:*",
    "@the-brain/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
